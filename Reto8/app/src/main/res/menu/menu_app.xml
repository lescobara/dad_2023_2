<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <item
        android:id="@+id/action_nuevo"
        android:icon="@drawable/new_24"
        android:title="Nuevo"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_buscar"
        android:icon="@drawable/search_24"
        android:title="Buscar"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/action_buscar_nombre"
                android:title="Buscar por nombre" />
            <item
                android:id="@+id/action_buscar_clasificacion"
                android:title="Buscar por clasificación" />
        </menu>
    </item>
</menu>
